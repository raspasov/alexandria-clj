(ns ss.react-native-lib.orientation-locker
 ;(:require [react-native-orientation-locker :as -locker])
 )

;(def ^js/Object locker -locker)
;(def Orientation (.-default locker))
;
;
;(comment
; (.lockToPortrait Orientation)
;
; (.getInitialOrientation Orientation)
;
; (.unlockAllOrientations Orientation))
